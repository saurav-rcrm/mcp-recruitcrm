{
  "name": "recruitcrm-mcp-server",
  "version": "0.4.3",
  "description": "Recruit CRM MCP Server",
  "type": "module",
  "bin": {
    "recruitcrm-mcp": "dist/index.js",
    "recruitcrm-mcp-server": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "test": "vitest run",
    "test:smoke": "vitest run tests/smoke.test.ts",
    "dev": "tsx src/index.ts"
  },
  "keywords": [
    "mcp",
    "recruitcrm",
    "recruit-crm"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
